MEMORY {
    MAIN:     start = $3000, size = $0800, fill = yes, fillval = $ff;
    MAIN2:    start = $3800, size = $0800, fill = yes, fillval = $ff;
}
SEGMENTS {
    CODE:     load = MAIN, type = rw;
    MONITOR:  load = MAIN2, type = rw;
}

